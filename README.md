# 07.11.2025
Вариант 12. Создайте функцию для генерации всех сочетаний размера k из n элементов.

python

как работает алгоритм: 
k = 0 -  Возвращает список с одним пустым сочетанием
k > len(l) - невозможно, вернуть пусто
Для каждого элемента списка:
Фиксируем текущий элемент
Из оставшихся элементов рекурсивно генерируем сочетания размера k-1
Добавляем фиксированный элемент к каждому полученному сочетанию
Ключевой принцип:
Каждое сочетание размера k состоит из:
Текущего элемента + сочетание размера k-1 из оставшихся элементов

Временная сложность:
O(C(n,k) × k) - как и у стандартных реализаций
Где C(n,k) - количество сочетаний

Почему временная сложность O(C(n,k) × k)
1. Количество узлов в дереве рекурсии:
2. Стоимость создания каждого сочетания:
3. Рекурсивные вызовы

С++

Как работает алгоритм:
Копирование векторов: new_comb.insert() и comb.push_back() требуют O(k)
k == 0 → возвращает {{}} (одно пустое сочетание)
k > l.size() → возвращает {} (невозможно)
Рекурсивная часть:
Для каждого элемента в списке
Создает подмассив rem_items из оставшихся элементов
Рекурсивно генерирует сочетания размера k-1 из оставшихся
добавляет текущий элемент к каждому полученному сочетанию

Временная сложность:
O(C(n,k) × k)

Почему временная сложность O(C(n,k) × k)
1. Количество сочетаний: C(n,k) = n!/(k!(n-k)!)
Алгоритм генерирует ровно C(n,k) сочетаний
2. Стоимость создания одного сочетания: O(k)
3. Общая стоимость: C(n,k) × k операций

Java 

Как работает алгоритм:
l.subList(i + 1, l.size()) - O(1) (представление, не копирование)
k == 0 → возвращает список с одним пустым списком
k > l.size() → возвращает пустой список
Рекурсивная часть:
Для каждого элемента в списке
Создает подсписок remItems из оставшихся элементов
Рекурсивно генерирует сочетания размера k-1 из оставшихся
Добавляет текущий элемент к каждому полученному сочетанию

Временная сложность:
O(C(n,k) × k)

Почему такая сложность:
1. Количество сочетаний: C(n,k) = n!/(k!(n-k)!)
Алгоритм генерирует ровно C(n,k) сочетаний
2. Стоимость создания одного сочетания: O(k)
3. Общая стоимость: C(n,k) × k операций

   Сколько существует перестановок множества из n элементов? ответь нормально
Количество перестановок множества из n элементов
Ответ: n! (n-факториал)
Формула:
P(n) = n! = n × (n-1) × (n-2) × ... × 3 × 2 × 1
Примеры:
n = 3: 3! = 3 × 2 × 1 = 6 перестановок
n = 4: 4! = 4 × 3 × 2 × 1 = 24 перестановки
n = 5: 5! = 5 × 4 × 3 × 2 × 1 = 120 перестановок
Особые случаи:
n = 0: 0! = 1 перестановка (пустое множество)
n = 1: 1! = 1 перестановка (единственный элемент)
Обоснование:
Для первого элемента есть n вариантов выбора, для второго - (n-1) вариантов, и так далее до последнего элемента, для которого остаётся 1 вариант.


